---

- name: Prepare nginx configuration
  template:
    src: nginx.conf.j2
    dest: "/etc/nginx/nginx.conf"
    owner: root
    group: root
    mode: '0644'

- name: Enable and start nginx service
  systemd:
    name: nginx
    enabled: yes
    masked: no
    state: started
    daemon_reload: yes
